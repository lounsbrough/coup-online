name: Setup Valkey

runs:
  using: composite
  steps:
    - name: Install Valkey packages
      run: sudo apt-get update && sudo apt-get install -y valkey-tools valkey
      shell: bash
    - name: Verify Valkey connectivity
      run: valkey-cli ping
      shell: bash
